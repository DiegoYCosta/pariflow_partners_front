part of '../app.dart';

class _LegacyShellPage extends StatelessWidget {
  const _LegacyShellPage();

  @override
  Widget build(BuildContext context) {
    return const _ShellPreviewPage(variant: _ShellVariant.legacy);
  }
}
